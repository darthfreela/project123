<section class="content-header">
    <h1>Gerenciamento Tipo de Serviço
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i>Home</a></li>
        <li><a href="#">Ferramentas do usuário</a></li>
        <li class="active">Gerenciamento Tipo de Serviço</li>
    </ol>
</section>
<section class="content">
    <div class="box collapsed-box">
        <div class="box-header">
            <h3 class="box-title">Cadastro Tipo de Serviço</h3>
            <div class="box-tools pull-right">
                <button type="button" class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-plus"></i></button>
            </div>
        </div>
        <hr>
        <div class="alert-error">
            <%= flash[:error] %>
            <% if @tpServico.errors.any? %>
                <div id="error_explanation">
                    O formulário contém <%= pluralize(@tpServico.errors.count, "erro") %>.
                </div>
                <ul>
                    <% @tpServico.errors.full_messages.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                </ul>
            <% end %>
        </div>
        <%= form_for @tpServico do |f| %>
            <div class="box-body">
                <div class="col-md-2 form-group ">
                    <label>Sigla*</label>
                    <%= f.text_field :initials, PlaceHolder: "Sigla", maxlength: "6", class:"form-control" %>
                </div>
                <div class="col-md-8 form-group ">
                    <label>Nome do Serviço*</label>
                    <%= f.text_field :name, PlaceHolder: "Nome", maxlength: "80", class:"form-control" %>
                </div>
                <div class="col-md-4 form-group">
                    <label>Função*</label>
                    <%= f.select :function_id, options_for_select(@select_funcao), {}, class: "form-control" %>
                </div>
                <div class="col-md-4 form-group">
                    <label>Uniformes*</label>
                    <%= f.select :uniform_id, options_for_select(@select_uniformes), {}, class: "form-control" %>
                </div>
                <div class="col-md-2 form-group">
                    <label>Ativo*</label>
                    <%= f.select :actived, [['Sim', true], ['Não', false]],{}, class: "form-control" %>
                </div>
            </div>
            <div class="box-footer">
                <%= f.submit "Salvar", class: "btn btn-success"%>
                <%= f.button "Limpar", class: "btn btn-danger"%>
                <div class=" small pull-right">* Campos obrigatórios</div>
            </div>
        <% end %>
    </div>
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Listar Tipo de Serviço</h3>
            <div class="box-tools pull-right">
                <button type="button" class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-minus"></i></button>
            </div>
        </div>
        <div class="box-body">
            <div class="input-group input-group-sm">
                <input type="text" class="form-control">
                <span class="input-group-btn">
                    <button type="button" class="btn btn-info btn-flat">Pesquisar</button>
                </span>
            </div>
        </div>
        <hr>
        <div class="box-body">
            <% if @tpServico_show.count < 1%>
                Não existem tipos de serviços cadastrados.
            <% else %>
                <table id="motivos" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th></th>
                            <th>Sigla</th>
                            <th>Nome</th>
                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @tpServico_show.each do |o|%>
                            <tr>
                                <td><%= o.id %></td>
                                <td><%= o.initials %></td>
                                <td><%= o.name %></td>
                                <td><%= link_to raw("<i class='fa fa-edit'>Editar</i>"), {:controller => 'service_types', :action => 'edit', :id => o.id}, class: 'btn btn-warning', method: :get %></td>
                                <td><%= link_to raw("<i class='fa fa-close'>Remover</i>"), {:controller => 'service_types', :action => 'destroy', :id => o.id},:data => {:confirm=> "Tem certeza que deseja remover este registro?"}, class: 'btn btn-danger', method: :delete %></td>
                            </tr>
                        <% end %>
                    </tbody>
                </table>
            <% end %>
        </div>
        <div class="box-footer">
        </div>
    </div>
</section>
